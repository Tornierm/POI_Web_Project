import { CategoryInternationalFilterPipe } from './category-international-filter.pipe';

describe('CategoryInternationalFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new CategoryInternationalFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
