import { CategoryEconomyFilterPipe } from './category-economy-filter.pipe';

describe('CategoryEconomyFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new CategoryEconomyFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
