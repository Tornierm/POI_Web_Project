import { CategorySportsFilterPipe } from './category-sports-filter.pipe';

describe('CategorySportsFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new CategorySportsFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
